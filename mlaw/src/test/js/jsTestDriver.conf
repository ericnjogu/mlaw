# should be the same port as the one used to start the server
server: http://localhost:9876

# the paths below assume the tests will be run with a --basePath flag that points to / (the root of the project)
# use a jquery file that will not change regardless of the name of the rice war

load:
  - src/test/js/jquery-1.7.2.js
  - src/main/webapp/scripts/mlaw.js
  - src/test/js/mlaw.test.js